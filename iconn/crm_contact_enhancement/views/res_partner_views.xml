<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_partner_form_address_order" model="ir.ui.view">
        <field name="name">res.partner.form.address.order</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_form" />
        <field name="priority" eval="1000" />
        <field name="arch" type="xml">
            <xpath expr="//div[contains(@class, 'o_address_format') and not(@name='div_address')]" position="replace">
                <div class="o_address_format">
                    <field name="country_enforce_cities" invisible="1"/>
                    <div name="partner_address_country" class="d-flex justify-content-between">
                        <field name="country_id" placeholder="Country" class="o_address_country" options="{'no_open': True, 'no_create': True}"
                            readonly="type == 'contact' and parent_id"/>
                    </div>
                    <div class="o_address_row">
                        <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}"
                            readonly="not country_id or (type == 'contact' and parent_id)" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                        <field name="city_id"
                            placeholder="City"
                            class="o_address_city"
                            domain="[('country_id', '=', country_id)]"
                            invisible="not country_enforce_cities"
                            readonly="type == 'contact' and parent_id"
                            context="{'default_country_id': country_id, 'default_state_id': state_id, 'default_zipcode': zip}"/>
                        <field name="city" placeholder="City" class="o_address_city"
                            invisible="country_enforce_cities and (city_id or city in ('', False))"
                            readonly="type == 'contact' and parent_id"/>
                        <field name="zip" placeholder="ZIP" class="o_address_zip"
                            readonly="type == 'contact' and parent_id"/>
                    </div>
                    <field name="street" placeholder="Street..." class="o_address_street"
                        readonly="type == 'contact' and parent_id"/>
                    <field name="street2" placeholder="Street 2..." class="o_address_street"
                        readonly="type == 'contact' and parent_id"/>
                </div>
            </xpath>
            <xpath expr="//div[contains(@class, 'o_address_format') and @name='div_address']" position="replace">
                <div class="o_address_format" name="div_address">
                    <field name="country_enforce_cities" invisible="1"/>
                    <field name="country_id" placeholder="Country" class="o_address_country" options="{'no_open': True, 'no_create': True}"/>
                    <div class="o_address_row">
                        <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}"
                            readonly="not country_id" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                        <field name="city_id"
                            placeholder="City"
                            class="o_address_city"
                            domain="[('country_id', '=', country_id)]"
                            invisible="not country_enforce_cities"
                            context="{'default_country_id': country_id, 'default_state_id': state_id, 'default_zipcode': zip}"/>
                        <field name="city" placeholder="City" class="o_address_city"
                            invisible="country_enforce_cities and (city_id or city in ('', False))"/>
                        <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                    </div>
                    <field name="street" placeholder="Street..." class="o_address_street"/>
                    <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                </div>
            </xpath>
        </field>
    </record>

    <record id="view_partner_address_form_address_order" model="ir.ui.view">
        <field name="name">res.partner.address.form.address.order</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.view_partner_address_form" />
        <field name="priority" eval="1000" />
        <field name="arch" type="xml">
            <xpath expr="//div[contains(@class, 'o_address_format')]" position="replace">
                <div class="o_address_format">
                    <field name="country_id" placeholder="Country" class="o_address_country" options="{'no_open': True, 'no_create': True}"/>
                    <div class="o_address_row">
                        <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}"
                            readonly="not country_id" context="{'default_country_id': country_id}"/>
                        <field name="city" placeholder="City" class="o_address_city"/>
                        <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                    </div>
                    <field name="street" placeholder="Street..." class="o_address_street"/>
                    <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                </div>
            </xpath>
        </field>
    </record>

    <record id="view_partner_private_form_address_order" model="ir.ui.view">
        <field name="name">res.partner.form.private.address.order</field>
        <field name="model">res.partner</field>
        <field name="inherit_id" ref="base.res_partner_view_form_private" />
        <field name="priority" eval="1000" />
        <field name="arch" type="xml">
            <xpath expr="//div[contains(@class, 'o_address_format') and @name='div_address']" position="replace">
                <div class="o_address_format" name="div_address">
                    <field name="country_enforce_cities" invisible="1"/>
                    <field name="country_id" placeholder="Country" class="o_address_country" options="{'no_open': True, 'no_create': True}"/>
                    <div class="o_address_row">
                        <field name="state_id" class="o_address_state" placeholder="State" options="{'no_open': True, 'no_quick_create': True}"
                            readonly="not country_id" context="{'country_id': country_id, 'default_country_id': country_id, 'zip': zip}"/>
                        <field name="city_id"
                            placeholder="City"
                            class="o_address_city"
                            domain="[('country_id', '=', country_id)]"
                            invisible="not country_enforce_cities"
                            context="{'default_country_id': country_id, 'default_state_id': state_id, 'default_zipcode': zip}"/>
                        <field name="city" placeholder="City" class="o_address_city"
                            invisible="country_enforce_cities and (city_id or city in ('', False))"/>
                        <field name="zip" placeholder="ZIP" class="o_address_zip"/>
                    </div>
                    <field name="street" placeholder="Street..." class="o_address_street"/>
                    <field name="street2" placeholder="Street 2..." class="o_address_street"/>
                </div>
            </xpath>
        </field>
    </record>
</odoo>
