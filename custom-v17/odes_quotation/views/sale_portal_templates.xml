<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <template id="sale_order_portal_content_inherit" inherit_id="sale.sale_order_portal_content" name="Sales Order Portal Content" priority="1">
        <xpath expr="//table[@id='sales_order_table']" position="replace">
            <table t-att-data-order-id="sale_order.id" t-att-data-token="sale_order.access_token" class="table table-sm" id="sales_order_table">
                <colgroup t-if="sale_order.quotation_type == 'service'">
                    <col width="63%"/>
                    <col width="22%"/>
                    <col width="15%"/>
                </colgroup>
                <thead class="bg-100">
                    <tr>
                        <t t-if="sale_order.quotation_type != 'service'">
                            <th class="text-center">Vendor</th>
                            <th class="text-left">Products Description</th>
                            <th class="text-center">Qty</th>
                            <th t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">U/Price</th>
                            <th t-if="display_discount" t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                <span>Disc.%</span>
                            </th>
                            <!-- <th t-attf-class="text-right {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                <span>Taxes</span>
                            </th> -->
                            <th class="text-center">
                                <span> Total Price (<span t-field="sale_order.currency_id.name"/>)</span>
                                <!-- <span groups="account.group_show_line_subtotals_tax_excluded">Amount</span> -->
                                <!-- <span groups="account.group_show_line_subtotals_tax_included">Total Price</span> -->
                            </th>
                            <th t-attf-class="text-right {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                <span>L/T (ARO)</span>
                            </th>
                        </t>
                        <t t-if="sale_order.quotation_type == 'service'">
                            <th>Task / Descriptions</th>
                            <th class="text-center">Consultant Man-days</th>
                            <th class="text-right">Cost</th>
                        </t>
                    </tr>
                </thead>
                <tbody class="sale_tbody">

                    <t t-set="current_subtotal" t-value="0"/>

                    <t t-foreach="sale_order.order_line" t-as="line">

                        <t t-set="current_subtotal" t-value="current_subtotal + line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                        <t t-set="current_subtotal" t-value="current_subtotal + line.price_total" groups="account.group_show_line_subtotals_tax_included"/>

                        <tr t-att-class="'bg-200 font-weight-bold o_line_section' if line.display_type == 'line_section' else 'font-italic o_line_note' if line.display_type == 'line_note' else ''">
                            <t t-if="not line.display_type">
                                <t t-if="sale_order.quotation_type != 'service'">
                                    <td name="account_invoice_line_name"><span t-field="line.product_id.product_brand_id.name" t-options="{'widget': 'text'}"/></td>
                                    <td class="text-left"><span t-field="line.product_id.name" t-options="{'widget': 'text'}"/></td>
                                    <td class="text-center">
                                        <span class="text-nowrap" t-field="line.product_uom_qty" groups="account.group_show_line_subtotals_tax_excluded" t-options='{"widget": "float", "precision": 0}'/>
                                        <span class="text-nowrap" t-field="line.price_total" groups="account.group_show_line_subtotals_tax_included"/>
                                    </td>
                                    <td t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <div
                                            t-if="line.discount &gt;= 0"
                                            t-field="line.price_unit"
                                            t-att-style="line.discount and 'text-decoration: line-through' or None"
                                            t-att-class="(line.discount and 'text-danger' or '') + ' text-right'"
                                        />
                                        <div t-if="line.discount">
                                            <t t-esc="(1-line.discount / 100.0) * line.price_unit" t-options='{"widget": "float", "decimal_precision": "Product Price"}'/>
                                        </div>
                                    </td>
                                    <td t-if="display_discount" t-attf-class="text-right {{ 'd-none d-sm-table-cell' if report_type == 'html' else '' }}">
                                        <strong t-if="line.discount &gt; 0" class="text-info">
                                            <t t-esc="((line.discount % 1) and '%s' or '%d') % line.discount"/>%
                                        </strong>
                                    </td>
                                    <!-- <td t-attf-class="text-right {{ 'd-none d-md-table-cell' if report_type == 'html' else '' }}">
                                        <span t-esc="', '.join(map(lambda x: (x.description or x.name), line.tax_id))"/>
                                    </td> -->
                                    <td class="text-right">
                                        <span class="oe_order_line_price_subtotal" t-field="line.price_subtotal" groups="account.group_show_line_subtotals_tax_excluded"/>
                                        <span class="oe_order_line_price_total" t-field="line.price_total" groups="account.group_show_line_subtotals_tax_included"/>
                                    </td>
                                    <td class="text-center o_lead_time">
                                        <span t-esc="int(line.sale_delay)"/> weeks
                                    </td>
                                </t>
                                <t t-if="sale_order.quotation_type == 'service'">
                                    <td id="product_name" class="service_quotation">
                                        <span t-field="line.name" t-options='{"widget": "html"}' class="quotation-html"/>
                                    </td>
                                    <td class="text-center">
                                        <div id="quote_qty">
                                            <t t-if="line.service_line_type == 'day'">
                                                <span t-esc="int(line.product_uom_qty)"/>
                                            </t>
                                            <t t-if="line.service_line_type == 'off'">
                                                NA
                                            </t>
                                        </div>
                                    </td>
                                    <td class="text-right">
                                        <span class="oe_order_line_price_subtotal" t-field="line.price_subtotal"/>
                                    </td>
                                </t>
                            </t>
                            <t t-if="line.display_type == 'line_section'">
                                <td colspan="99">
                                    <span t-field="line.name"/>
                                </td>
                                <t t-set="current_section" t-value="line"/>
                                <t t-set="current_subtotal" t-value="0"/>
                            </t>
                            <t t-if="line.display_type == 'line_note'">
                                <td colspan="99">
                                    <span t-field="line.name"/>
                                </td>
                            </t>
                        </tr>

                        <t t-if="current_section and (line_last or sale_order.order_line[line_index+1].display_type == 'line_section')">
                            <tr class="is-subtotal text-right">
                                <td colspan="99">
                                    <strong class="mr16">Subtotal</strong>
                                    <span
                                        t-esc="current_subtotal"
                                        t-options='{"widget": "monetary", "display_currency": sale_order.pricelist_id.currency_id}'
                                    />
                                </td>
                            </tr>
                        </t>
                    </t>
                </tbody>
            </table>
        </xpath>
    </template>
</odoo>
