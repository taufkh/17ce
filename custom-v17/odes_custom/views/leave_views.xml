<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="odes_custom_view_hr_leave_view_form_manager_inherit" model="ir.ui.view">
        <field name="name">hr.leave.view.form.inherit</field>
        <field name="model">hr.leave</field>
        <field name="inherit_id" ref="hr_holidays.hr_leave_view_form_manager"/>
        <field name="arch" type="xml">
            <field name="name" position="after">
                <!-- <group>
                    <field name="is_manager" readonly="1"/>
                    <field name="is_finance" readonly="1"/>
                    <field name="is_director" readonly="1"/>
                    <field name="approver_id" domain="[('share', '=', False)]" widget="many2one_avatar_user" string="Approver" readonly="1" force_save="1" />
                    <field name="date_approve" readonly="1"/>

                </group> -->
            </field>
        
        </field>
    </record>

    <record id="odes_custom_hr_leave_view_form" model="ir.ui.view">
        <field name="name">odes.custom.hr.leave.view.form</field>
        <field name="model">hr.leave</field>
        <field name="inherit_id" ref="hr_holidays.hr_leave_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//field[@name='name']/.." position="after">
                <group>
                    <field name="is_manager" readonly="1"/>
                    <field name="is_finance" readonly="1"/>
                    <field name="is_director" readonly="1"/>
                    <field name="approver_id" domain="[('share', '=', False)]" widget="many2one_avatar_user" string="Approver" readonly="1" force_save="1" />
                    <field name="date_approve" readonly="1"/>
                </group>
            </xpath>
        </field>
    </record>

    <record id="hr_holidays.view_hr_holidays_filter" model="ir.ui.view">
        <field name="name">hr.holidays.filter</field>
        <field name="model">hr.leave</field>
        <field name="arch" type="xml">
            <search string="Search Time Off">
                <field name="employee_id"/>
                <field name="department_id" operator="child_of"/>
                <field name="holiday_status_id"/>
                <field name="name"/>
                <filter domain="[('state','in',('confirm','validate1')),('first_approver_id.user_id','=',uid)]" string="To Approve" name="approve"/>
                <filter domain="[('state', '=', 'validate1')]" string="Need Second Approval" name="second_approval"/>
                <filter string="Approved Time Off" domain="[('state', '=', 'validate')]" name="validated"/>
                <separator/>
                <filter string="My Department Time Off" name="department" domain="['|', ('department_id.member_ids.user_id', '=', uid), ('employee_id.user_id', '=', uid)]" help="My Department Time Off"/>
                <filter string="People I Manage" name="managed_people" domain="['|', ('employee_id.leave_manager_id', '=', uid), ('user_id', '=', uid)]" help="Time off of people you are manager of"/>
                <filter string="My Time Off" name="my_leaves" domain="[('employee_id.user_id', '=', uid)]"/>
                <!-- v16: payslip_status removed — field not available in Community edition -->
                <separator/>
                <filter name="filter_date_from" date="date_from"/>
                <separator/>
                <filter name="year" string="Active Time Off"
                    domain="[('holiday_status_id.active', '=', True)]" help="Active Time Off"/>
                <filter invisible="1" string="Late Activities" name="activities_overdue"
                    domain="[('activity_ids.date_deadline', '&lt;', context_today().strftime('%Y-%m-%d'))]"
                    help="Show all records which has next action date is before today"/>
                <filter invisible="1" string="Today Activities" name="activities_today"
                    domain="[('activity_ids.date_deadline', '=', context_today().strftime('%Y-%m-%d'))]"/>
                <filter invisible="1" string="Future Activities" name="activities_upcoming_all"
                        domain="[('activity_ids.date_deadline', '&gt;', context_today().strftime('%Y-%m-%d'))
                        ]"/>
                <group expand="0" string="Group By">
                    <filter name="group_employee" string="Employee" context="{'group_by':'employee_id'}"/>
                    <filter name="group_type" string="Type" context="{'group_by':'holiday_status_id'}"/>
                    <filter name="group_state" string="Status" context="{'group_by': 'state'}"/>
                    <separator/>
                    <filter name="group_date_from" string="Start Date" context="{'group_by':'date_from'}"/>
                </group>
                <searchpanel>
                    <field name="state" string="Status"/>
                </searchpanel>
            </search>
        </field>
    </record>

    <record id="odes_custom.odes_custom_action_action_approve_department" model="ir.actions.act_window">
        <field name="name">All Time Off</field>
        <field name="res_model">hr.leave</field>
        <field name="view_mode">tree,kanban,form,calendar,activity</field>
        <field name="search_view_id" ref="hr_holidays.hr_leave_view_search_manager"/>
        <field name="context">{
            'search_default_managed_people': 0,
            'search_default_approve': 1,
            'hide_employee_name': 1}
        </field>
        <field name="domain">[]</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Meet the time off dashboard.
            </p><p>
                A great way to keep track on employee’s PTOs, sick days, and approval status.
            </p>
        </field>
    </record>

</odoo>
