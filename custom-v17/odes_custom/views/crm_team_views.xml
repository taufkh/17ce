<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- <record id="odes_crm_team_view_tree_inherit" model="ir.ui.view">
        <field name="name">odes.crm.team.view.tree.inherit</field>
        <field name="model">crm.team</field>
        <field name="inherit_id" ref="sales_team.crm_team_view_tree"/>
        <field name="arch" type="xml">
            <field name="company_id" position="after">
                <field name="stage_ids" widget="many2many_tags"/>
            </field>
        </field>
    </record> -->

    <record id="odes_crm_team_view_form" model="ir.ui.view">
        <field name="name">odes.crm.team.view.form.inherit</field>
        <field name="model">crm.team</field>
        <field name="inherit_id" ref="sales_team.crm_team_view_form"/>
        <field name="arch" type="xml">
            <!-- <xpath expr="//field[@name='currency_id']" position="after">
                <field name="stage_ids" widget="many2many_tags" required="1" options="{'no_open': 1}" domain="[('team_id', '=', False)]"/>
            </xpath> -->

            <xpath expr="//field[@name='member_ids']" position="after">
                <!-- <field name="new_member_ids" required="1" options="{'no_open': 1}" domain="[('team_id', '=', False)]">
                    <kanban quick_create="false" create="true" delete="true">
                        <field name="id"/>
                        <field name="name"/>
                        <templates>
                            <t t-name="kanban-box">
                                <div class="oe_kanban_global_click" style="max-width: 200px">
                                    <div class="o_kanban_record_top">
                                        <img t-att-src="kanban_image('res.users', 'image_128', record.id.raw_value)" class="oe_avatar oe_kanban_avatar_smallbox o_image_40_cover mb0" alt="Avatar"/>
                                        <div class="o_kanban_record_headings ml8">
                                            <strong class="o_kanban_record_title"><field name="name"/></strong>
                                        </div>
                                    </div>
                                </div>
                            </t>
                        </templates>
                    </kanban>
                </field> -->
            </xpath>
        </field>
    </record>
</odoo>