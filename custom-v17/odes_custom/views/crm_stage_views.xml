<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="odes_crm_stage_tree_form" model="ir.ui.view">
        <field name="name">odes.crm.stage.view.tree.inherit</field>
        <field name="model">crm.stage</field>
        <field name="inherit_id" ref="crm.crm_stage_tree"/>
        <field name="arch" type="xml">
            <field name="team_id" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
        </field>
    </record>
    
    <record id="odes_crm_stage_form_form" model="ir.ui.view">
        <field name="name">odes.crm.stage.view.form.inherit</field>
        <field name="model">crm.stage</field>
        <field name="inherit_id" ref="crm.crm_stage_form"/>
        <field name="arch" type="xml">
            <field name="team_id" position="attributes">
                <attribute name="invisible">1</attribute>
            </field>
            <field name="is_won" position="after">
                <field name="is_need_so"/>
                <field name="is_confirm_so"/>
                <field name="is_revenue"/>
                <field name="company_ids" widget="many2many_tags" options="{'no_create': 1}"/>
            </field>
        </field>
    </record>

</odoo>